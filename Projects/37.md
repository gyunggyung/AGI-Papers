---
id: 37
category: Projects
title: 얼마 전 공개했던 HybriKo 아키텍처(RNN+Attention) 117M 모델을 A100 8장으로 Pre-training하고, 리눅스 명령어 Function Calling 태스크로 SFT 했습니다. Claude와 Gemini로 5,000개의 Function Calling 데이터 생성하자, 실제로 쓸만한 결과가 나왔습니다.
---
얼마 전 공개했던 HybriKo 아키텍처(RNN+Attention) 117M 모델을 A100 8장으로 Pre-training하고, 리눅스 명령어 Function Calling 태스크로 SFT 했습니다. Claude와 Gemini로 5,000개의 Function Calling 데이터 생성하자, 실제로 쓸만한 결과가 나왔습니다. 

제가 로컬과 Colab에서 PoC 제작 및 테스트 후 A100에서 학습할 계획서를 만들고, 홍석님께서 해당 계획서를 바탕으로 코드를 만들어 A100에서 실제 학습을 해주셨습니다. 아무런 자원이 없는 저에게 A100 8장의 서버를 써서 같이 작품을 만들어주신 Hongseok Lee 홍석님 덕분에 해당 작업이 가능했습니다. 다시 한 번 감사드립니다!

📊 결과
250개 샘플 학습: Action Name 정확도 4% (2/50)
5,000개 샘플 학습: Action Name 정확도 100% (100/100)
Eval Loss도 0.039에서 0.0039로 10배 감소했습니다.

🧠 아키텍처
직접 설계한 Griffin-style Hybrid 구조입니다.
- 2:1 패턴: Griffin Block(RG-LRU) 2개 + Attention Block(GQA+RoPE) 1개 반복
- 12 레이어 (Griffin 8 + Attention 4)
- 117.8M 파라미터

🎯 지원 명령어 (21종)
ls, cd, mkdir, rm, cp, mv, find, cat, grep, head, tail, wc, ps, df, du, top, ping, curl, chmod, tar, Finish

⚡ Colab에서 20초면 테스트가 가능합니다.
```
!pip install -q huggingface_hub sentencepiece
from huggingface_hub import hf_hub_download
hf_hub_download("Yaongi/HybriKo-117M-LinuxFC-SFT-v2", "demo_colab.py", local_dir=".")

!python demo_colab.py --query "디스크 사용량을 확인해줘"
```

⚠️ 한계점
소량의 데이터를 학습시킨 117M 소형 모델의 한계로 파라미터에서 hallucination이 발생합니다. 예를 들어 "test.txt 삭제해줘"라고 해도 {"path": "test.py"}처럼 파일명이 바뀌어 생성되기도 합니다. Action Name(어떤 명령어를 쓸지)은 잘 맞추지만, 세부 파라미터는 아직 개선이 필요합니다.

애초에 Pre-training 자체도 소규모 데이터로 학습하고, SFT도 5,000개 샘플만 사용했기 때문에 개선할 여지가 많습니다. 데이터를 늘리거나 모델 크기를 키우면 해결될지 확인해볼 예정입니다.

💡 인사이트
소형 모델에서도 충분한 양의 고품질 데이터가 있으면 특정 태스크에서 뛰어난 성능을 보여줄 수 있습니다. 250개에서 5,000개로, 20배 데이터 증가가 4%에서 100%로의 도약을 만들었습니다.

실제 사용 시에는 모델이 어떤 명령어를 쓸지만 맞추면 파라미터는 파이썬 코드로 보정할 수 있어, 실용적으로는 충분히 활용 가능합니다.

Apache 2.0 라이선스로 공개합니다.



29




